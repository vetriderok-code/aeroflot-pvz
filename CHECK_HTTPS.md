# Проверка работы HTTPS

## SSL сертификаты успешно настроены!

### Что было сделано:

1. ✅ SSL сертификаты получены (действительны до 2026-04-22)
2. ✅ HTTPS редирект включен в nginx
3. ✅ Контейнер nginx перезапущен

### Проверка работы:

#### 1. Проверка HTTP редиректа:
```bash
curl -I http://aeroflot-pvz.ru
```
Ожидается: `HTTP/1.1 301 Moved Permanently` с заголовком `Location: https://aeroflot-pvz.ru/`

#### 2. Проверка HTTPS:
```bash
curl -I https://aeroflot-pvz.ru
```
Ожидается: `HTTP/2 200` или другой успешный статус

#### 3. Проверка в браузере:
Откройте: **https://aeroflot-pvz.ru**

Должен отображаться зеленый замочек (SSL активен).

### Текущая конфигурация:

- ✅ HTTP (порт 80) → автоматический редирект на HTTPS
- ✅ HTTPS (порт 443) → работает с SSL сертификатами
- ✅ Домены: aeroflot-pvz.ru, www.aeroflot-pvz.ru

### Важно:

**Автоматическое обновление**: DNS challenge сертификаты не обновляются автоматически.
Для обновления перед истечением срока (2026-04-22) выполните ту же команду получения сертификатов.

### Настройки Django:

Убедитесь что в `.env` файле установлено:
```
USE_HTTPS=True
```
(если DEBUG=False)

---

**Все готово! Сайт работает через HTTPS!** 🎉
