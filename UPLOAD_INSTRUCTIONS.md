# Инструкция по загрузке файлов на сервер

## 📍 Текущий путь к проекту (локально):

```
C:\Users\reshe\Downloads\Telegram Desktop\rubicon_bot_docker — export
```

## 📍 Путь на сервере (куда загружать):

```
/opt/rubicon
```

## 📦 Варианты загрузки:

### Вариант 1: Автоматический (рекомендуется) - через PowerShell скрипт

1. **Создайте архив с нужными файлами:**
   ```powershell
   # В PowerShell в директории проекта
   .\create-upload-package.ps1
   ```

2. **Загрузите архив на сервер:**
   ```powershell
   scp rubicon-production-*.zip root@46.17.40.216:/opt/rubicon/
   ```

3. **На сервере распакуйте:**
   ```bash
   ssh root@46.17.40.216
   cd /opt/rubicon
   unzip rubicon-production-*.zip
   ```

### Вариант 2: Ручная загрузка через SCP

```powershell
# Из PowerShell в директории проекта
cd "C:\Users\reshe\Downloads\Telegram Desktop\rubicon_bot_docker — export"

# Загрузите директории и файлы
scp -r api root@46.17.40.216:/opt/rubicon/
scp -r nginx root@46.17.40.216:/opt/rubicon/
scp -r rubicon_admin root@46.17.40.216:/opt/rubicon/
scp docker-compose.prod.yaml root@46.17.40.216:/opt/rubicon/
scp env.prod.example root@46.17.40.216:/opt/rubicon/
scp get-ssl-cert.sh root@46.17.40.216:/opt/rubicon/
scp deploy.sh root@46.17.40.216:/opt/rubicon/

# Если есть video_files (опционально)
scp -r video_files root@46.17.40.216:/opt/rubicon/
```

### Вариант 3: Через Git (если проект в репозитории)

```bash
# На сервере
ssh root@46.17.40.216
mkdir -p /opt/rubicon
cd /opt/rubicon
git clone your_repository_url .
# или если уже есть репозиторий
git pull origin main
```

## ✅ Обязательные файлы и директории:

```
✅ docker-compose.prod.yaml    - Конфигурация Docker Compose для продакшна
✅ env.prod.example            - Пример переменных окружения
✅ get-ssl-cert.sh             - Скрипт получения SSL сертификата
✅ deploy.sh                   - Скрипт развертывания
✅ api/                        - Директория API (Dockerfile, requirements.txt, entrypoint.sh)
✅ nginx/                      - Конфигурация Nginx (Dockerfile, conf.d/prod.conf)
✅ rubicon_admin/              - Django приложение (вся директория)
```

## ❌ Файлы, которые НЕ нужно загружать:

```
❌ .env                        - Локальные переменные (создается на сервере)
❌ .env.prod                   - Создается на сервере из env.prod.example
❌ docker-compose.yaml         - Локальная версия (не нужна)
❌ .git/                       - Если используете git на сервере
❌ __pycache__/                - Кеш Python
❌ *.pyc                       - Скомпилированные файлы
❌ .redis_data/                - Данные Redis
❌ *.log                       - Логи
❌ *.md                        - Документация (опционально)
❌ *.bat                       - Windows скрипты
```

## 🔍 Проверка после загрузки:

```bash
# На сервере проверьте наличие файлов
ssh root@46.17.40.216
cd /opt/rubicon
ls -la

# Должны быть:
# - docker-compose.prod.yaml
# - env.prod.example
# - get-ssl-cert.sh
# - deploy.sh
# - api/
# - nginx/
# - rubicon_admin/
```

## 📝 Следующие шаги после загрузки:

1. **Создайте .env.prod:**
   ```bash
   cd /opt/rubicon
   cp env.prod.example .env.prod
   nano .env.prod  # Заполните все значения
   ```

2. **Создайте директории для Certbot:**
   ```bash
   mkdir -p certbot/conf certbot/www
   ```

3. **Сделайте скрипты исполняемыми:**
   ```bash
   chmod +x get-ssl-cert.sh deploy.sh
   ```

4. **Следуйте инструкциям из QUICK_START.md**

## 📊 Размер проекта (приблизительно):

- `api/` - ~1-2 MB
- `nginx/` - ~100 KB
- `rubicon_admin/` - ~5-10 MB
- Итого: ~10-15 MB (без видео файлов)

## 🚀 Быстрая команда для загрузки (если используете Git):

```bash
# На сервере
cd /opt/rubicon
git clone https://your-repo-url.git .
# или
git pull
```











